from datetime import datetime
now = datetime.now()
current_time = now.strftime("%H:%M:%S")
print("Current time =", current_time)
n = input("Pick up key? (y/n)")
if n == "y":
    print("You picked up the key!")
else:
    print("This key's required, so I don't know why you aren't picking it up. ")
# I wanted to have the hatch only open when the key is in the players inventory, but I'm unsure as to how I would do
# that

s = input("pick up key? (y/n)"):
if s = "y":
    key = obtained
u = input("Open hatch")
